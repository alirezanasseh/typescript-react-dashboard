import {IUser, IUserDTO} from './user.interface';
import {IPerm} from './perm.interface';
import {
    IRole,
    IUserRole
} from './role.interface';

export type {
    IUser,
    IUserDTO,
    IPerm,
    IRole,
    IUserRole
}