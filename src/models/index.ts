import {UserModel} from './user.model';
import {PermModel} from './perm.model';

const Models = [
    {model: UserModel, icon: 'ImUsers'},
    {model: PermModel, icon: 'MdVpnKey'}
];

export default Models;